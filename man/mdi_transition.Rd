% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mdi_transition.R
\name{mdi_transition}
\alias{mdi_transition}
\title{Count Classification Switches Between Consecutive Periods}
\usage{
mdi_transition(DT, id, time, classvar)
}
\arguments{
\item{DT}{A data.table containing the panel data. Modified in place by
sorting on `id` and `time` before processing.}

\item{id}{Character. Name of the unit identifier column (e.g. `"firmid"`).}

\item{time}{Character. Name of the time variable column (e.g. `"year"`).}

\item{classvar}{Character. Name of the classification variable column
(e.g. `"nace"`).}
}
\value{
A data.table with four columns: `old_code`, `new_code`,
  `year_shifted`, and `N` (count of units making that switch in that year).
}
\description{
Identifies pairs of classification codes between which a unit switched in a
given year, and counts how often each specific switch is observed across
units. The output has one row per (old code, new code, year) triple.
}
\examples{
library(data.table)
DT <- data.table(
  firmid = c("A", "A", "A", "B", "B", "B"),
  year   = c(2010L, 2011L, 2012L, 2010L, 2011L, 2012L),
  nace   = c("C10", "C10", "C20", "D30", "D40", "D40")
)
mdi_transition(DT, id = "firmid", time = "year", classvar = "nace")
}
