% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mdi_wdrg_prodest.R
\name{mdi_wdrg_prodest}
\alias{mdi_wdrg_prodest}
\title{Wooldridge (2009) System-GMM Production Function Estimator}
\usage{
mdi_wdrg_prodest(
  DT,
  y,
  endog,
  exog,
  instr,
  id,
  time,
  degree = 2,
  tol = 1e-10,
  TFP_demeaned = TRUE,
  TFP_minuend = "y"
)
}
\arguments{
\item{DT}{A \code{data.table} (or coercible object) containing panel data.}

\item{y}{Character. Output variable name (in logs).}

\item{endog}{Character. Free input name (e.g. labour; one variable for now).}

\item{exog}{Character. State input name (e.g. capital; one variable for now).}

\item{instr}{Character. Proxy variable name (e.g. materials).}

\item{id}{Character. Firm identifier column.}

\item{time}{Character. Time identifier column.}

\item{degree}{Integer. Polynomial degree for c(x, m). Default \code{2}.}

\item{tol}{Numeric. Linear-solver tolerance for the GMM normal equations.
Default \code{1e-10}.}

\item{TFP_demeaned}{Logical. If \code{TRUE}, returns TFP_demeaned
(tfp - period mean). Default \code{TRUE}.}

\item{TFP_minuend}{Character. Currently only \code{"y"} is supported.}
}
\value{
A \code{data.table} with one row per observation in the GMM sample,
  containing \code{id}, \code{time}, \code{tfp}, \code{alpha_hat},
  \code{el_<input>}, \code{se_el_<input>}, \code{NumObs}, and optionally
  \code{TFP_demeaned}.
}
\description{
Implements the stacked two-equation GMM from Wooldridge (2009, Economics
Letters 104, 112-114). The system is estimated linearly in the parameters:
the polynomial coefficients on \eqn{(x_t, m_t)} (Wooldridge eq 3.3) and on
\eqn{(x_{t-1}, m_{t-1})} (eq 3.4) are free, separate vectors. This is more
general than the random-walk-with-drift restriction (eq 3.10) which forces
them equal, but it does NOT impose the structural rho_g * lambda^g form
that eq (3.4) implies for a general degree-G polynomial law of motion
(G > 1, which would require nonlinear GMM and is not implemented here).
The implementation matches the standard empirical Wooldridge GMM used in
Petrin, Poi and Levinsohn (2004).

Returns elasticities common to all firms in the group, firm-year
\code{tfp = y - X*beta} (the intercept \code{alpha_hat} is reported as a
separate column for diagnostics, not subtracted), and analytical SEs.
}
\examples{
\donttest{
library(data.table)
set.seed(1)
n <- 200
DT <- data.table(
  id   = rep(1:50, each = 4),
  year = rep(2000:2003, times = 50),
  y    = rnorm(n, 5, 1),
  l    = rnorm(n, 3, 0.5),
  k    = rnorm(n, 4, 0.5),
  m    = rnorm(n, 2, 0.5)
)
result <- mdi_wdrg_prodest(DT, y = "y", endog = "l", exog = "k", instr = "m",
                           id = "id", time = "year", TFP_demeaned = FALSE)
}

}
